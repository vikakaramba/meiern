{
  "name": "common",
  "version": "1.0.0",
  "description": "common packages for server and client",
  "types": "lib/**/*.d.ts",
  "files": [
    "lib/**/*.ts"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ts-watch": "tsc -w",
    "build": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^3.8.3"
  },
  "devDependencies": {
    "prettier": "2.0.4"
  }
}
